<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <title>LuxuryStay - 予約フォーム</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Roboto:wght@400;500&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-gray-100">
  <header class="glass-nav fixed top-0 left-0 right-0 z-20">
    <div class="header flex flex-col sm:flex-row items-center justify-between">
      <div class="flex items-center">
        <img src="/logo.png" alt="Logo" class="h-10 mr-3">
        <span class="logo-text text-2xl">LuxuryStay</span>
      </div>
      <nav class="flex space-x-4 mt-2 sm:mt-0">
        <a href="/" class="nav-link">ホーム</a>
        <a href="/hotel" class="nav-link">ホテル詳細</a>
        <a href="/contact" class="nav-link">お問い合わせ</a>
        <a href="/register" class="nav-link">登録</a>
        <a href="/login" class="nav-link">ログイン</a>
      </nav>
    </div>
  </header>

  <main class="max-w-3xl mx-auto pt-24 px-4">
    <div class="bg-white rounded-xl shadow-lg overflow-hidden mb-8">
      <img src="<%= hotel.image %>" alt="<%= hotel.name %>" class="w-full h-64 object-cover">
      <div class="p-6">
        <h1 class="text-3xl font-bold mb-2"><%= hotel.name %></h1>
        <p class="text-gray-600 mb-4"><%= hotel.location %></p>
      </div>
    </div>
    <% if (error) { %>
      <div class="mb-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded text-center"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="mb-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded text-center"><%= success %></div>
    <% } %>
    <div class="bg-white rounded-xl shadow-lg p-6">
      <h2 class="text-2xl font-bold mb-4">予約フォーム</h2>
      <form action="/booking" method="POST" class="space-y-4">
        <input type="hidden" name="hotelId" value="<%= hotel.id %>">
        <div>
          <label for="checkin" class="block text-gray-700 font-bold mb-2">チェックイン日</label>
          <input type="date" id="checkin" name="checkin" class="form-input" required>
        </div>
        <div>
          <label for="checkout" class="block text-gray-700 font-bold mb-2">チェックアウト日</label>
          <input type="date" id="checkout" name="checkout" class="form-input" required>
        </div>
        <div>
          <label for="guests" class="block text-gray-700 font-bold mb-2">人数</label>
          <input type="number" id="guests" name="guests" class="form-input" min="1" value="1" required>
        </div>
        <div>
          <label for="roomType" class="block text-gray-700 font-bold mb-2">部屋タイプ</label>
          <select id="roomType" name="roomType" class="form-select" required>
            <option value="">選択してください</option>
            <option value="standard">スタンダード</option>
            <option value="deluxe">デラックス</option>
            <option value="suite">スイート</option>
          </select>
        </div>
        <div class="text-center">
          <button type="submit" class="form-btn">予約する</button>
        </div>
      </form>
    </div>
  </main>

  <footer class="bg-gray-900 text-gray-300">
    <div class="footer text-center">
      <p>&copy; 2025 LuxuryStay. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
